/* The one easing, everywhere. Mirrors --ease in tokens.css and the CustomEase
   "brand" registered in motion/index.ts. Exported as a bare cubic-bezier string
   so the always-on (GSAP-free) path can use it with the Web Animations API. */
export const EASE = "cubic-bezier(0.22, 1, 0.36, 1)";
